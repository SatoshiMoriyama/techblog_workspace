lint エラーが0件になりました。

修正内容のまとめ:

- MD060（table-column-style）エラーが3箇所ありました
  - 25行目、131行目、143行目のテーブルセパレータ行（`|------|-----|`）にスペースがなく、データ行（`| 項目 | 値 |`）のスタイルと不一致だった
  - セパレータ行にスペースを追加して統一（`| ------ | ----- |`）
- 文章の内容には一切変更を加えていません